/**
 * @file Serves as the public API for the 'auth' feature.
 * It exports the main components that other parts of the application (like the `app` layer)
 * can import and use. This follows the principles of modular architecture.
 */
export { LoginForm } from "./components/LoginForm";
